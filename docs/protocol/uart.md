# UART 协议

串口接收以回车或换行结束的一行文本。当前只接受 MIT 目标命令：

```text
m位置deg[,目标速度deg/s[,前馈力矩mN·m]]
```

例如 `m10`、`m90,0,0`、`m90,0,5`。裸整数也按目标位置处理，如 `30` 等价于 `m30`。当前解析器只接受整数，不接受小数；旧的 `p90`、`s20` 和标准三环模式命令不再使用。

串口端口、引脚和波特率以 `source/src/usart.c` 为准。运行状态输出含状态、位置误差、速度、MIT 输出力矩、目标 `Iq` 和实测 `Iq` 等字段。
